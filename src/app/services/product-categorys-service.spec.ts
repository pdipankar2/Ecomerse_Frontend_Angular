import { ProductCategorysService } from './product-categorys-service';

describe('ProductCategorysService', () => {
  it('should create an instance', () => {
    expect(new ProductCategorysService()).toBeTruthy();
  });
});
